package main;

/**
 * A graph that supports the calculation of distances between nodes.
 *
 * @param <V> the type of nodes in this graph
 */
public interface MetricGraph<V> extends MetricSpace<V>, Graph<V> {

}
